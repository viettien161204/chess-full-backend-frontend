{
    "env": {
      "browser": true,
      "worker": true // Thêm môi trường worker
    },
    "rules": {
      "no-restricted-globals": ["error", { "name": "self", "message": "Use self only in Web Worker context" }]
    }
  }